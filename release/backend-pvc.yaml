apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-data
  labels:
    io.kompose.service: backend
spec:
  storageClassName: local-path
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
